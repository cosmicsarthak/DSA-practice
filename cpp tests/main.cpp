#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define endl "\n"

int main()
{

    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--)
    {
        cin >> n >> q;
        int n, q;
        // cerr << "nm__" << n << " " << m << endl;

        vector<int> v;
        int nn = n;
        while (nn--)
        {
            int tmp;
            cin >> tmp;
            v.emplace_back(tmp);
        }
    }
    // cosmicsarthak
    return 0;
}
